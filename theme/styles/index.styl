
@require './palette' // vuepress 理论上会自动加载 palette.styl ，但加载顺序好像太慢
@require './base'
@require './code'
@require './custom-blocks'

@require './wrapper'
@require './keyframe'





// 清除浮动
.clearfix:after {
    content: "";
    display: table;
    clear: both;
    overflow: hidden
}

// 文字超出 显示省略号
.ellipsis {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap
}

// 链接样式
a.text--link
    position relative
    text-decoration none
    color rgb(52, 73, 94)
    &::after
      content ''
      width 100%
      height 1px /*设置伪元素的高度，这里是下划线的粗细*/
      position absolute
      top 100%
      left 0
      background-color currentColor /*当前标签继承的文字颜色，这里让伪元素的背景色与父元素的文字颜色相同*/
      transform scale(0)
      transition all .2s
    &:hover::after
      transform scale(1)
      transform-origin center

@require 'mobile.styl'
